{
  "name": "India_In_China",  //Change this to the name of your project
  "containerEnv": {
    "API_KEY": "${localEnv:API_KEY}",
    "CSE_ID": "${localEnv:CSE_ID}"
  },
  "postCreateCommand": "pip install -r requirements.txt"
}
